# ChatApp (PWA + Socket.IO)

A simple realtime chat app built with React (frontend) and Express + Socket.IO (backend).  
Works on desktop + mobile, installable as a PWA.

---

## 🚀 How to run locally

1. Clone the repo:
   ```bash
   git clone https://github.com/YOUR_USERNAME/chatapp.git
   cd chatapp
